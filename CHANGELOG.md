# Homebrew Updater — Changelog

## 0.1.0
- Initial scaffold. Tracks a list of homebrew apps (GitHub `owner/name` + the
  `.nro` path on your SD card), checks each repo's latest GitHub release, and
  updates the `.nro` in place when a newer version is available.
